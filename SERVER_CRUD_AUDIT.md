# Minecraft.kr 서버 등록·운영자 백엔드 감사 보고서

검증일: 2026-07-13

## 감사 결론

초기 `codex2`에는 브리지 상태 수집 API만 존재했고, 서버 등록 폼은 데이터를 저장하지 않았습니다. 서버 디렉터리 테이블, 소유자별 조회, 정보 수정, 삭제, 운영자 관리 화면과 이미지 영구 저장이 빠져 있었습니다.

감사 과정에서 누락 기능을 구현하고 실제 로컬 D1·R2 요청과 브라우저 운영자 화면으로 검증했습니다.

## 구현 범위

### 등록 데이터

- 서버 제목: 2~60자
- 목록 한 줄 소개: 2~80자
- 서버 상세 설명: 20~10,000자
- 에디션: Java, Bedrock, Java + Bedrock
- 최소 버전과 최대 버전: 각 1~24자
- 서버 호스트와 포트: 호스트 형식 및 1~65535 범위 검증
- 카테고리: 허용 목록에서 1~5개
- 동일 호스트·포트 중복 등록 차단
- 신규 서버 상태: `draft`

### 이미지 저장

- 아이콘 `256×256`, 최대 1MB
- 데스크톱 목록 `1200×200`, 최대 950KB, GIF 지원
- 데스크톱 상세 `1440×480`, 최대 950KB, GIF 지원
- 모바일 목록 `750×300`, 최대 950KB, GIF 지원
- 모바일 상세 `750×500`, 최대 950KB, GIF 지원
- PNG/JPG/WebP 헤더와 실제 픽셀을 서버에서 재검사
- D1에 이미지 메타데이터, R2 `MEDIA`에 원본 바이트 저장
- 등록 중 이미지 저장 실패 시 생성된 서버 레코드 자동 롤백

### 소유권과 보안

- 배포 환경: `oai-authenticated-user-email` 서버 헤더 사용
- 로컬 환경: localhost 전용 테스트 소유자 헤더
- 비인증 수정 요청 `401`
- 다른 소유자의 레코드 수정·삭제 `403`
- 비공개 초안 서버는 소유자가 아니면 `404`
- 삭제 시 서버 제목 정확 입력 요구

### 브리지 연결

- 운영자가 Paper/Bukkit 또는 Velocity를 선택해 일회용 키 발급
- 디렉터리 서버와 `bridge_servers` 연결
- 발급 후 상태를 `pending_verification`으로 변경
- 실제 MOTD 검증 성공 시 디렉터리 상태를 `active`로 변경
- 주소·포트 수정 시 기존 브리지 ID, nonce, 백엔드 텔레메트리와 자격 증명 폐기
- 서버 삭제 시 브리지 데이터와 R2 이미지 함께 제거

## 실제 검증 결과

| 검증 항목 | 예상 | 결과 |
|---|---:|---:|
| 서버 등록 | 201 | 통과 |
| 소유자 서버 조회 | 200 | 통과 |
| 비인증 수정 차단 | 401 | 통과 |
| 제목·상세 설명·버전 수정 | 200 | 통과 |
| 동일 주소 중복 등록 차단 | 409 | 통과 |
| 잘못된 삭제 확인문 차단 | 400 | 통과 |
| 정상 삭제 | 204 | 통과 |
| 삭제 후 조회 | 404 | 통과 |
| 브리지 키 발급 | 201 | 통과 |
| 주소 변경 후 브리지 폐기 | 404 | 통과 |
| 정확한 이미지 5종 R2 저장 | 200 | 통과 |
| 잘못된 이미지 픽셀 차단 | 400 | 통과 |
| 운영자 화면 데이터 로딩 | 1개 표시 | 통과 |
| 운영자 화면 제목·설명·버전 저장 | 저장 메시지 및 재렌더링 | 통과 |
| 운영자 화면 삭제 | 0개 빈 상태 | 통과 |

자동 검증 결과: 린트 통과, 프로덕션 빌드 통과, 렌더링·접근성 테스트 3/3 통과, `server CRUD smoke` 통과.

Paper/Bukkit·Velocity 플러그인의 실제 기동과 서명·재전송 방어 검증은 기존 `BRIDGE_VERIFICATION_REPORT.md`에 보존되어 있습니다.
